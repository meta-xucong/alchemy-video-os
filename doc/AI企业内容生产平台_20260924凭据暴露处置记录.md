# AI 企业内容生产平台：2026-09-24 凭据暴露处置记录

版本：`1.1.0`

日期：`2026-09-24`

状态：`REPOSITORY_HISTORY_SANITIZED / EXTERNAL_CREDENTIAL_ROTATION_REQUIRED`

## 1. 事件范围

静态审计发现旧主线提交 `36a7ceba08f2c62fbf9f78e77e56c6973ce5f07c` 跟踪了本地启动脚本：

```text
apps/control-api/start-control-api.sh
```

脚本包含三类真实凭据：SUB2API 视频访问密钥、参考图视觉服务密钥、参考图交付签名密钥。本记录不保存、不复述任何原始值。

旧验收 PR #1 即使删除该文件，也可能在删除行中展示原值，因此不能继续作为验收入口。

## 2. 已完成的仓库处置

1. 立即关闭旧 PR #1，并明确其不得继续作为审计入口；
2. 在本地创建安全备份引用，防止业务改动丢失；
3. 从旧主线末次提交的父节点重建等价业务提交，排除凭据脚本；
4. 在 `.gitignore` 增加精确规则 `apps/control-api/start-control-api.sh`；
5. 对重建提交的树与差异执行凭据模式扫描；
6. 仅在确认远端 `main` 未变化后，以 `force-with-lease` 更新远端主线；
7. 净化后主线固定为 `1a4d95ba9e79e04540c1f7af61b917053735d1e5`；
8. 将 G01 审计分支重放到净化后主线；
9. 验证当前主线树、审计分支树和二者差异均不包含已识别原始凭据；
10. 新验收 PR 将从净化后主线创建，旧 PR 保持关闭。

## 2.1 远端备份分支的明确处置

远端仍保留：

```text
origin/codex/backup-20260919-snapshot@12446154b88e95cfd5bab84f10d52b4d574af5da
```

该分支不是旧验收 PR #1 的 head，也不是 PR #2 的基线。调查确认它是 2026-09-19 创建、与 `main`/PR #2 无共同祖先的孤立 WIP 文件快照；含 546 个文件，其中 19 个路径不在 PR #2，92 个文件内容不同。直接删除可能丢失独立工作，因此本轮决定：

- 明确保留为 `RETAINED_NOT_ACCEPTANCE`；
- 不作为净化主线、审计基线、验收入口或直接合并对象；
- 删除前必须由仓库所有者确认独有内容不再需要；
- 旧远端审计分支“已删除”仅指 `codex/audit-snapshot-20260924`；
- PR #2 仍是唯一有效验收入口。

定向扫描未发现该归档包含已知凭据启动脚本；模式命中为测试 token 形状和文档/示例占位符。该结果不是对归档分支的全面安全认证，任何未来恢复或合并都必须重新做完整 secret scan 和逐文件审查。

## 3. 外部账号所有者必须完成的动作

仓库净化不等于凭据轮换。对应账号/环境所有者必须：

- 吊销并重建 SUB2API 视频访问密钥；
- 吊销并重建参考图视觉服务密钥；
- 轮换参考图交付签名密钥，并同步所有合法运行环境；
- 复核暴露窗口内的 API 调用、费用、来源 IP、失败率和异常用量；
- 验证旧密钥全部失效；
- 在验收工单中保存轮换时间、操作人、后台审计截图或等价日志；
- 必要时向 GitHub 支持申请清理不可见缓存/对象，并确认旧提交和旧 PR 不再作为共享入口。

本地执行器没有这些外部账号控制台的授权，因此不得把上述动作写成已完成。

## 4. 验收影响

- 代码、文档和仓库历史可以继续独立审计；
- 新 PR 不应包含原始凭据或凭据脚本；
- 在轮换证明和用量日志复核提交前，安全验收只能标记为 `EXTERNAL_CREDENTIAL_ROTATION_REQUIRED`；
- 该外部门不阻止审计代码正确性，但阻止“生产安全全部关闭”的结论；
- 不得因为历史净化成功而重新启用旧凭据。

## 5. 验收人检查项

- [ ] `origin/main` 指向净化基线；
- [ ] 新 PR base 为净化后 `main`；
- [ ] 新 PR 不包含 `apps/control-api/start-control-api.sh`；
- [ ] `.gitignore` 有精确忽略规则；
- [ ] 当前树和 PR diff 的 secret scan 无命中；
- [ ] 旧 PR #1 已关闭；
- [ ] `codex/audit-snapshot-20260924` 已删除；`codex/backup-20260919-snapshot` 已明确分类为非验收 WIP 归档，未被误写成净化基线；
- [ ] 三类凭据已由账号所有者轮换；
- [ ] 旧凭据失效和暴露窗口用量已复核；
- [ ] 轮换证明已归档；
- [ ] 未执行未经授权的生产部署。
