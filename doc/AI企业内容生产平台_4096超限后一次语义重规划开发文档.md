# AI 企业内容生产平台：4096 超限后一次语义重规划开发文档

> 状态：`ACCEPTED`（仅本地 MVP 窄切片；不等同整体平台或 C12.4/C12.5 验收）
>
> 本文是《AI企业内容生产平台_原仓库原则驱动的4096UTF8保真保护性压缩开发文档》的受控增量。它只增加“提示词预算预检失败后，复用现有智能分段再规划一次”的协调行为；不替换原压缩规则，也不改变公共契约、任务状态或 Provider 协议。

## 1. 用户目标与问题边界

用户希望：当现有智能规划得到的某个生成片段在 Provider 的 4096 UTF-8 字节预检中无法通过时，程序再尝试一次是否可以把该片段拆成两个独立、语义完整的片段，并分别重新编译提示词；成功则继续制作，仍无法通过则明确阻断。

当前行为是：Workflow Worker 完成 `StoryboardPlan -> PromptPackage` 后才由 Production Worker 的 runtime-profile 做 4096 预检。若源内容或保留的必要内容无法安全压缩，任务在 Provider 提交前以 `PROMPT_BUDGET` 阻断；现有 planner 不读取提示词字节预算，因此不会再次规划。

本方案只处理新规划尚未落库/尚未创建 Provider TaskRun 的阶段。已经创建的不可变 `TaskRun.input_snapshot` 不原地修改；已经提交 Provider 的任务不回溯拆分、不重复提交、不重复扣费。

## 2. 固定来源与现有实现映射

| 来源/固定版本 | 可复用事实 | 本地对应实现 | 本次适配 |
| --- | --- | --- | --- |
| `huobao-drama@f04d705603bd0257bcec6b8f44fd04ea3ea9b795` | `backend/workspace/skills/storyboard-breaker/SKILL.md`、`prompt-generator/video-prompt/SKILL.md`：以源段落/动作边界组织分镜，保持台词和顺序，不按字符机械切片；一个段落可承载多个子镜头 | `packages/creative-planning/src/index.ts` 的 `chooseGenerationSegmentCount`、`planSegmentDurations`、`distributeDialogueLines`、`distributeEvents` | 仅增加一个内部“最低段数”输入，让同一套边界/容量/时长规则再运行一次；不新增字符切片或词速算法 |
| `Seedance-2.5@ebc68d3c19a62fba0f9ba9d2805af1f711a82aa7` | `skill/seedance-25/SKILL.md`、`references/prompting.md`、`references/references.md`：可见 progression、global locks、引用标签和输入顺序保持，维度只设一个 owner | 现有 `StoryboardShotSpec`、`MotionBeat`、`referencePolicy`、PromptPackage sidecar | 重规划后仍使用已有 reference/continuity/motion 规则，不复制完整源文到每个新片段 |
| `OpenMontage@4eab34c5cfcccaa4f1970554928feccce73ee930` | `lib/shot_prompt_builder.py:build_shot_prompt`、`skills/creative/video-gen-prompting.md`：按已填写的字段组成短提示词；没有通用 4096 压缩或自动拆段实现 | `packages/creative-planning` 的确定性编译器与 `packages/provider-video/src/runtime-profile.ts` | 预算触发属于平台内部协调适配；OpenMontage 不被声称提供该算法 |
| 本地既有预算实现 | `compactRuntimePrompt`、`UnsupportedVideoGenerationInputError.code = PROMPT_BUDGET`、`capabilitySnapshot.source_prompt/generated_prompt_parts` | `packages/provider-video/src/runtime-profile.ts`、Workflow sidecar 传递链 | 只调用既有预检结果决定是否重规划；不改 compactor、marker、白名单、阈值或错误语义 |

## 3. 冻结的最小流程

### 3.1 第一次规划与预检

1. Workflow Worker 按现有 `PlanningModelPort.plan` 生成计划。
2. 按现有 `DeterministicStoryboardCompiler` 编译每个 `PromptPackage`，保留现有 `source_prompt` 与 `generated_prompt_parts` 私有 sidecar。
3. 仅当当前 runtime profile 明确是 `sub2api` 且有既有有效 Provider prompt ceiling 时，使用 `compactRuntimePrompt` 对每个编译结果做同一预算预检：
   - 已在 ceiling 内：原样通过，不触发重规划；
   - 可依据 sidecar 省略派生块或既有白名单后通过：原样通过，不触发重规划；
   - 以既有 `UnsupportedVideoGenerationInputError` 且 `code === "PROMPT_BUDGET"` 失败：进入一次重规划；
   - 其它错误：沿用原错误，不转换为重规划。
4. 所有片段预检通过后，才执行现有 `completeCreativePlan` 持久化和后续 outbox/队列流程。

Mock/缺少 Provider capability 的规划不触发该重规划；默认 Mock 仍按本地规则工作。

### 3.2 一次语义重规划

1. 记录本次规划已使用一次 `prompt-budget-replan`，禁止递归或第三次尝试。
2. 以第一次结果的 `generationSegmentCount + 1` 作为内部 `minimumGenerationSegmentCount`，再次调用同一个现有 planner。该字段只存在于 `PlanningInput` 内部类型，不进入公开 HTTP、数据库 schema、事件或浏览器 DTO。
3. `chooseGenerationSegmentCount` 先执行原有目标时长、场景、叙事点和口播容量规则，再取不小于 `minimumGenerationSegmentCount` 的结果；最终仍由既有 domain/persistence storyboard invariant 校验合法时长和顺序。
4. 重规划必须重新分配：
   - `NarrativeBeat`/`MotionBeat` 到每个独立片段；
   - 每段口播行和段落边界；
   - 每段时长、开始/结束状态和依赖；
   - 现有 `REFERENCE_SET`、`HANDOFF_FIRST_FRAME`、`TEXT_TRANSITION` 策略。
5. 重新编译所有片段并再次执行同一 `compactRuntimePrompt` 预检：
   - 全部通过：只持久化第二次完整规划；
   - 任一片段仍返回 `PROMPT_BUDGET`：沿用既有预算阻断；
   - 出现其它规划/契约错误：沿用原错误。

### 3.3 时长和语义约束

- 不固定为 7.5 秒；时长由现有 `durationPolicy` 和 `planSegmentDurations` 分配。Grok 的 `1..15` 与默认 Huobao 的 `8..15` 仍按已解析 profile 决定，不能互相冒充。
- 不按字符、字数、浏览器行数或字节位置切源；只使用已有段落、标点、场景、动作结束姿态和口播容量边界。
- 一个不可再分的源单元即使超过预算也必须保持完整并最终阻断，不能为了凑字节截断。
- 不把完整原提示词复制到每个新片段；每个片段只带自己拥有的 source、dialogue、motion 和引用事实。全局锁按现有编译器规则保留必要内容。

## 4. 不变量与安全边界

- **Provider/计费边界**：两次规划和所有预算预检都发生在 `completeCreativePlan` 之前；没有 Provider submit、轮询、下载、媒体 Runtime 或扣费副作用。
- **不可变性**：第一次失败的未持久化草稿不产生正式 revision；已有 revision/TaskRun 不被覆盖。后续用户重试通过既有新 revision/新运行路径处理。
- **幂等性**：Workflow 事件仍使用既有 command/event 幂等；一次执行内部最多两次 `plan`，重启后的事件回放不创建第二个 revision。
- **来源 sidecar**：预算预检只消费已存在的 `capabilitySnapshot` sidecar；缺失或不匹配沿用 `PROMPT_BUDGET` fail-closed，不按字符串猜 provenance。
- **模块边界**：不修改 `packages/provider-video/src/runtime-profile.ts`、公开 contracts、DB schema、任务状态、Billing、Provider adapter、Studio API、VPS 或 Sub2API。

## 5. 允许改动文件与唯一写入边界

本执行切片唯一写入者可修改：

- 本文档及本章审计记录的新增条目；
- `packages/creative-planning/src/index.ts`：仅增加内部 `minimumGenerationSegmentCount` 输入及其对既有段数选择的最小接线；
- `packages/creative-planning/tests/deterministic-planner.test.ts`：段数/边界回归；
- `apps/workflow-worker/src/execution-service.ts`：仅增加 Provider prompt budget 预检、一次重规划协调和 sidecar 读取；
- `apps/workflow-worker/src/index.ts`：仅把既有 runtime profile 的 mode/有效 ceiling 传给 executor；
- `apps/workflow-worker/tests/execution-service.test.ts`：预检、一次重规划、失败回退和无 Provider 副作用测试。

明确禁止修改：

- `packages/provider-video/src/runtime-profile.ts` 的压缩算法、阈值、marker 或错误码；
- `packages/contracts`、数据库 schema/migration、公开 API/SSE、TaskRun 状态、Provider 请求字段、Billing/Veyra；
- 前端 UI、字幕、TTS/BGM、参考图协议、VPS、Sub2API、真实 Provider、网络和 Git。

## 6. 行为测试与验收标准

### 6.1 必测场景

1. `sub2api` 初次所有片段预检通过：planner 调用一次，计划和 prompt sidecar 与现有结果一致。
2. 某片段初次返回 `PROMPT_BUDGET`、第二次增加一个语义片段后全部通过：planner 恰好调用两次；每段 source/dialogue 顺序完整；最终只持久化第二次 draft。
3. 第二次仍超限：planner 恰好调用两次，抛出 `PROMPT_BUDGET`，不调用 `completeCreativePlan`，不产生 outbox/Provider/Billing。
4. 初次错误不是 `PROMPT_BUDGET`：不重规划，原错误类型/消息沿用。
5. 缺 sidecar、sidecar 不匹配或不可表达源单元：只允许既有 fail-closed，不静默截断/重排。
6. Mock/未知 profile：不触发预算重规划；默认行为和现有测试保持。
7. 口播多行、中文 UTF-8、中文/ASCII 引号、`@anchor`、参考图顺序和 motion beat 归属在重规划后保持。
8. 默认 `8..15` 和已认证 Grok `1..15` 的合法时长由同一 `durationPolicy` 校验；不可形成合法段时沿用 `STORYBOARD_SPEC_INVALID`，不补长、不慢放、不造 7.5 秒硬阈值。
9. 一个不可分源单元超过 4096：一次重规划后仍阻断，不能按字符切开。

### 6.2 Exit Gate

只有同时满足以下条件才可提交 `READY_FOR_AUDIT`，不得自行标记 `ACCEPTED`：

- 文档、代码、测试差异均只在第 5 节范围；
- creative-planning 与 workflow-worker focused tests、typecheck、`git diff --check` 通过；
- 测试证明最多一次重规划、source/台词/引用顺序保真、非法时长 fail-closed 和 Provider/计费零副作用；
- 未修改 runtime-profile、contracts、schema、状态、事件、公开 API 或外部配置；
- 独立审计员对固定版本复核通过；数据库集成若因 `DATABASE_URL` 缺失而 skip，必须明确记录，不能用静态路径替代行为证据；
- 本地实际测试只在预检通过后由用户另行授权执行，当前文档实现阶段不调用真实 Provider/TTS/网络。

## 7. 已知限制

- 三个固定上游没有“4096 超限后自动重规划”的通用算法；本功能是平台在既有来源分段规则之上的一次内部协调薄壳，不能宣称原仓库原样提供。
- 只增加一个段数，不能保证极端长 source 一定可表达；不可表达时阻断是预期结果。
- 本方案只覆盖 Workflow 新规划路径；已有运行的恢复、已提交 Provider 的重试和历史快照不被改写。
- 字节预检成功不等于真实 Provider、音画或人工质量验收通过；真实调用和产物审核仍按既有授权/审计门禁执行。

## 8. 实施记录

- 基线：以本任务开始时工作区 `HEAD` 和未提交用户改动为准；不得回滚或覆盖。
- 设计审计：固定来源、范围、公共契约和“一次重规划”边界已由独立只读审计核对；首轮审计提出普通错误、sidecar 缺失和语义保真证据缺口，已补定向测试并由独立审计复核。
- 实现版本：工作区仅涉及 `packages/creative-planning/src/index.ts`、`packages/creative-planning/tests/deterministic-planner.test.ts`、`apps/workflow-worker/src/execution-service.ts`、`apps/workflow-worker/src/index.ts` 及 workflow 测试；既有 `apps/task-worker/*`、`packages/domain/*` 用户改动不属于本切片。
- 模拟测试：`pnpm --filter @alchemy-video/creative-planning test` = 59/59 pass、0 fail、0 skip；`pnpm --filter @alchemy-video/workflow-worker test` = 25/25 pass、0 fail、0 skip。新增覆盖一次预算重规划成功/失败、普通编译错误不重试、缺失/不匹配 sidecar fail-closed、Mock 单次执行，以及中文多行台词/源段落保序和每段时长边界；全为本地 fixture/mock，无 Provider、网络、计费或持久化副作用。
- 类型/格式检查：creative-planning 与 workflow-worker `typecheck` 通过；`git diff --check` 通过（仅保留既有 CRLF 警告）。
- 本地实际测试：在 `VIDEO_PROVIDER=mock` 下直接执行 Workflow Executor 测试入口（`pnpm --filter @alchemy-video/workflow-worker exec tsx --test tests/execution-service.test.ts`）= 17/17 pass、0 fail、0 skip；Docker daemon 当前未运行，因此未启动数据库/Redis 全服务 E2E。
- 最终独立审计：审计负责人 `madv1_audit_prompt_budget_replan_final` 对固定工作区版本给出 `ACCEPTED_ALLOWED`，确认本地窄切片满足 Exit Gate；未发现功能、范围或硬边界违规。审计同时确认数据库集成、真实 Provider、网络和 VPS 不属于本轮 Exit Gate，仍作为未验证边界；既有 `apps/task-worker/*`、`packages/domain/*` 与 `.workbuddy/*` 不属于本切片。
- GitHub/VPS：本地窄切片已满足可提交条件；任何提交只允许包含本切片文件，且必须排除现有用户改动、`.workbuddy`、密钥、媒体和测试输出。VPS 同步仍需沿项目部署/安全规则执行，不改变总体 C12.4/C12.5、E12/R01 或其它章节状态。
