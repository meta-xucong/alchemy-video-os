# C12 source record

- Source repository: `calesthio/OpenMontage`
- Fixed commit: `4eab34c5cfcccaa4f1970554928feccce73ee930`
- Reused concepts: `BaseTool` fixed-tool contract, `ToolResult` artifact/result separation, the video composition/QC tool boundaries, `tools/analysis/transcriber.py` faster-whisper/VAD/word timestamps, and `tools/analysis/video_understand.py` CLIP keyframe classification.
- Historical source-aligned explainer snapshots used a separately synthesized local Piper narration track as authoritative spoken audio; that statement is superseded for the current automatic flow. Current owner selection is native Provider first, or explicit Doubao only when chosen by the operator; Provider dialogue is not removed when a source-verified native owner is active.
- Platform adaptation: this Runtime exposes only three loopback-only, bounded byte-stream tools. It has no tool discovery, Agent orchestration, Backlot event file, project directory, external Provider, URL, or arbitrary command support. Platform persistence remains the only source of truth.
- Not copied: OpenMontage Python implementation, registry discovery, Backlot, provider integrations, filesystem project model, command-line passthrough, and global `.env` loading.
- Audio alignment update (C12.4/C12.5): follow OpenMontage `tools/audio/piper_tts.py` sentence-silence default `0.3` and natural `length_scale=1.0`. A C12.7B-approved narration asset is read as the authoritative bytes; when a READY plan carries independent formal asset versions for its PRIMARY sections, the Worker sends measured bytes and Runtime consumes them through the source `_full_mix` path with absolute starts. Cue-only multi-section requests and approved full-track section offsets still fail closed when those independent facts are absent. The platform preserves OpenMontage mixer fade/duck/loudnorm behavior and blocks unmusicked excessive tail silence rather than stretching with `atempo`/`rubberband`.
- Runtime certification: the optional C12.3 environment uses faster-whisper 1.2.1, CPU torch 2.13.0, and `openai/clip-vit-base-patch32` cached under the ignored local runtime directory. Missing models/dependencies still degrade to structured `UNAVAILABLE`; the platform does not claim pixel-perfect continuity from CLIP categories.
- Verification note: media-runtime Python tests must run with `services/media-runtime` as the working directory because the tests import local `main`/`runtime` modules.
- Accent boundary (compatibility): offline Piper remains available only when explicitly selected in a local fixture/compatibility request; it is not the current automatic narration owner. The current authorized local mode keeps Aiself Grok native audio first and uses the OpenMontage Doubao adapter only for an explicit operator replacement with a configured profile; timing parameters do not guarantee Mandarin accent quality.
- Thin source adapters: `adapters/openmontage_audio/piper.py` mirrors `tools/audio/piper_tts.py` command parameters/stdin contract; Runtime prefers the source `piper` executable and, only when the local installation exposes a module instead, invokes that same command with `python -m piper` (never the old `-i/-f` protocol). The Runtime subprocess timeout is `300` seconds, matching OpenMontage `PiperTTS._generate`. `selector.py` retains the source registry/provider metadata boundary; cloud entries are metadata-only and never perform network I/O. Full OpenMontage registry scoring remains outside this loopback Runtime, so no cloud provider is claimed as enabled.

The dated evidence entries below are migration history. Their older test counts and superseded cue-path descriptions are not current acceptance evidence; the latest source-first status and no-user-upload narration policy are governed by `doc/AI企业内容生产平台_自动生成音频与视频匹配正式使用开发文档.md` and the final reconciliation sections below.

## Current automatic narration input policy (2026-09-01)

Automatic narration does not require user-uploaded audio or samples. Samples are generated server-side by the selected native Provider or explicit Doubao route and must be manually approved before a separate formal narration asset is created. `NARRATION_SAMPLE` and `USER_SOURCE_AUDIO` remain compatibility roles only; generic reference, document and MUSIC uploads remain unchanged. The default local runtime stays Mock/Piper-compatible, while the user-authorized Aiself Grok/Doubao comparison is explicit and local-only.

## Current authorized local operation (2026-09-01)

The operator has authorized actual local Aiself Grok/Sub2API video calls and explicit Doubao TTS calls for the historical Maoshan project. Use Grok `grok-imagine-video-1.5` native audio first; choose Doubao `seed-tts-2.0` / `zh_female_meilinvyou_uranus_bigtts` only when replacing the native track. Credentials stay in the ignored process environment; repository templates and CI remain Mock and no user-uploaded narration/sample is introduced.

## 2026-08-30 C12.5 pace metadata adapter

- Historical note (superseded): an earlier adapter described `NATURAL -> 1.0`, `SLOW -> 1.15`, and `FAST -> 0.85`; OpenMontage exposes no symbolic pace enum, and the current source-faithful adapter rejects unmapped SLOW/FAST values before invoking Piper. No invented pace mapping is active.
- `pause_before_ms` remains an absolute-timeline assertion (the approved `start_ms` must already include it); `pause_after_ms` is passed as the source Piper `sentence_silence` value. Mixed pace profiles and non-neutral `energy` still fail closed because the upstream Piper interface has no safe per-section energy control.
- Regression evidence: `python -m unittest discover -s tests -p 'test_runtime.py' -v` — 75/75; `python -m unittest discover -s adapters -p 'test_*.py' -v` — 4/4. No real TTS or network call was made.

## 2026-08-30 C12.4 source-track filter and SSML boundary

- `upstream/openmontage/tools/audio/audio_mixer.py::_track_filters` applies source-local volume/fades before timeline scheduling. The Runtime now applies the equivalent numeric `gain_db` and non-zero `fade_in_ms`/`fade_out_ms` filters to explicitly classified `PROVIDER_AMBIENCE`/`USER_SOURCE_AUDIO`/`LEGACY_PRESERVE` tracks only while `CONTINUOUS_NARRATION` is active; the legacy policy remains untouched and `PROVIDER_DIALOGUE` is replaced as declared by AudioPlan.
- `provider_text` SSML `<break>` tags are not passed to Piper. The loopback cue-only path rejects any break as `MEDIA_RUNTIME_UNAVAILABLE` because Piper's sentence-silence flag cannot prove an arbitrary SSML duration; approved narration assets remain the only path that can carry independently verified timing. No `atempo`/`rubberband` or implicit padding was added.
- Regression evidence: Runtime `test_runtime.py` — 81/81, including source gain/fade filter-graph behavior, SSML break fail-closed behavior, explicit zero pause, provider-text transport, and truncated ALCHMED8 track-count bounds; adapter tests — 4/4.

## 2026-08-30 C12.4 caption burn and local runtime binding

- OpenMontage `tools/subtitle/subtitle_gen.py` cue grouping (bounded word/character cues with source word timestamps) is adapted by `_subtitle_srt_from_transcript`; `tools/video/remotion_caption_burn.py`'s FFmpeg `subtitles` fallback is exposed through the loopback `/internal/v1/media/captions` endpoint. The endpoint accepts an approved checked transcript snapshot or uses the configured local transcriber and marks the output with `alchemy_captions=burned_srt`; it never invents timings from script text.
- Production Worker invokes the burn endpoint when the persisted DeliveryPlan explicitly has `caption_policy=REQUIRED`, before final review. Final review reads the verified marker and reports `subtitle_check=CHECKED`; missing transcriber/timing remains an explicit retryable `MEDIA_RUNTIME_UNAVAILABLE` boundary rather than a false pass.
- The existing internal `alchemy_captions=burned_srt` marker is written with FFmpeg's `use_metadata_tags` MOV/MP4 muxer option so the same source-fallback output remains observable by the later ffprobe check; this is persistence of an existing fact, not a new caption protocol.
- `apps/control-api/tests/c12-local-e2e.mjs` now starts Media Runtime with the controlled C10 Python environment (and local `HF_HOME` cache), so faster-whisper/Piper capability is not accidentally evaluated in system Python. No external network or key is enabled.
- Current evidence: media-runtime `test_runtime.py` 87/87, adapters 4/4, production-worker 43/43, persistence unit mapper includes MUSIC gain default (`0` dB); caption endpoint/client and single-segment Provider-dialogue mute behavior are covered. The full Studio caption path still depends on an approved timing snapshot or installed faster-whisper model and remains pending audit.

## 2026-08-30 C12.4 controlled transcriber capability correction

- `transcriber_capability()` now treats `MEDIA_TRANSCRIBER_PYTHON_PATH` as a controlled-runtime assertion: when configured, it must resolve to the interpreter serving this Runtime, and that exact executable is probed for `faster_whisper` before capability is reported. The active `HF_HOME` is returned as capability context and inherited by the in-process source transcriber. A mismatched/missing interpreter or module is `UNAVAILABLE`; the Runtime never claims a different venv while importing from system Python.
- `apps/control-api/tests/c12-local-e2e.mjs` starts the Runtime with `.codex-longrun/c10-document-runtime-venv` when present and injects the same path for Piper/transcription plus the local HF cache. A system-Python fallback is video-only and cannot certify captions/TTS.
- Verification: `python -m unittest discover -s tests -p 'test_runtime.py' -v` from `services/media-runtime` — 87/87; `python -m unittest discover -s adapters -p 'test_*.py' -v` — 4/4. No real Provider/TTS/network call was made.

## 2026-08-30 C12.4/C12.5 verification correction

- The Worker final-review boundary now reconstructs a private canonical transcript from persisted cue/provider text when a cue-only snapshot has no top-level script field; empty cue text fails closed instead of bypassing script/ASR comparison.
- Continuous narration rejects an accepted source segment whose ownership is only `LEGACY_PRESERVE` and has no persisted delivery/ownership fact. This is the conservative OpenMontage ownership boundary: the runtime cannot safely decide whether unknown source audio is dialogue, ambience, or user audio, so it does not silently preserve or replace it.
- Latest evidence: production-worker 41/41, persistence 57/57 with explicit PostgreSQL, creative-planning 38/38, media-runtime 64/64, and root test 450 pass / 5 explicit skip / 0 fail after root typecheck/build. C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`; no real Provider/TTS/Veyra/network call was made.

## 2026-08-30 C12.4/C12.5 current transport and catalog boundary (12:38; historical snapshot)

- The current composition wire is ALCHMED8 for complete AudioPlan facts; ALCHMED1-7 remain compatibility readers. Approved full narration is still one measured track beginning at `t=0`; private section metadata is validated but not yet proven to drive full-track placement. Cue-only multi-section requests fail closed before Piper because their absolute offsets cannot be verified; only a single bounded cue uses the controlled one-shot Piper adapter.
- `/internal/v1/media/captions` now accepts bounded JSON `{video_base64, transcript?}` only, rejects raw MP4/malformed/oversized input, and performs checked word-timestamp duration/window validation before source `subtitles` FFmpeg burn. `BURN_CAPTIONS` is present in the internal tool contract; missing timing/transcriber capability remains `MEDIA_RUNTIME_UNAVAILABLE`.
- The local MVP has no Freesound/free-music catalog code, contract, UI or deployment/env injection. Music selection is limited to server-owned project `READY AUDIO` assets with `metadata.audio_role=MUSIC` or explicit OFF. OpenMontage/Pixabay references remain source documentation only; no network catalog or key is enabled.
- Current verification: Runtime `test_runtime.py` 90/90 (run from `services/media-runtime`), OpenMontage adapter tests 4/4, Contracts 36/36, Production Worker 43/43, Persistence 49 pass/10 explicit database-gated skips, Control API 45 pass/1 service-gated skip, Studio Web 36/36 + typecheck/build. Root `pnpm typecheck`, `pnpm build`, and serial `pnpm test` exit 0 with five explicit skips; no real Provider/TTS/Veyra/network call or Git commit.
- Historical note (12:55): the earlier multi-cue absolute-placement and PATH-only Piper checks were recorded before the minimal source-first correction below. The old per-cue fallback is no longer considered a supported production path.

## 2026-08-30 C12.4/C12.5 minimal source-first correction

- `approveNarrationScriptRevision` now records the existing sample approval event only. It does not insert the sample bytes into `narration_asset_versions` or emit `narration_asset_version.ready`; that event remains reserved for a measured formal full narration asset. Timeline readiness/loaders require an explicitly named non-sample version and reject a version that reuses the approved sample object.
- Worker composition sends one `scriptText` to the existing Piper adapter when a complete canonical script is available. Persisted cue windows remain review facts; they are never rendered as independent Piper jobs. Cue-only multi-section synthesis is blocked until OpenMontage `_full_mix` speech tracks with measured durations and absolute starts are wired.
- AUTO music now fails closed when no server-owned `READY AUDIO` with `audio_role=MUSIC` is available. `NARRATION_SAMPLE`, `USER_SOURCE_AUDIO`, and unclassified audio cannot satisfy the gate; only explicit OFF permits a silent music bed.
- Formal full narration with section windows that the current Runtime does not consume is rejected by the approved timeline loader. A single contiguous `PRIMARY` window from `t=0` is the only accepted full-track shape in this slice.
- Verification (superseded by the latest 14:22 run below): persistence `61 tests: 51 pass / 10 explicit database-gated skips`, production-worker `43/43`, media-runtime `92/92`; no real Provider/TTS/Veyra/network call was made. C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`; Chinese accent certification, subtitle/timestamp behavior, MIME truth, Studio formal-asset flow, and OpenMontage full-mix integration remain open hard gates.

## 2026-08-30 C12.4/C12.5 latest source-first verification (14:22)

- Source mapping was rechecked before each correction against the fixed OpenMontage explainer, Piper, mixer `_full_mix`, and voice-performance symbols above. The platform only retains thin workspace/object/event/error/queue adapters; no parallel narration algorithm, timing fallback, catalog, provider protocol, or UI meaning was added.
- Sample approval remains an approval event only; formal narration is an explicit independent measured asset version. The Worker sends one canonical script to the existing Piper adapter, while multi-cue/full-track windows fail closed until `_full_mix` absolute speech tracks are consumed by Runtime.
- AUTO music remains restricted to server-owned `AUDIO + READY + metadata.audio_role=MUSIC`; narration samples, user source audio, and unclassified audio cannot satisfy the music gate.
- Latest behavior evidence: Persistence `52 pass / 10 explicit DATABASE_URL-gated skip / 0 fail`; Production Worker `43/43`; Media Runtime `92/92`; Control API `47 pass / 1 explicit service-gated skip / 0 fail`. No external Provider/TTS/Veyra/network/VPS/Git operation was performed.
- C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`; approved section-level full-track windows, complete AudioPlan semantics, transition/xfade, cue-only full-narration-first, Studio sample/approval/TimelinePlan, REQUIRED subtitle fact chain, Mandarin accent certification, and overlong-consumer behavior evidence remain open hard gates.

## 2026-08-30 OpenMontage Pixabay source restoration

- By explicit user authorization, the Runtime now exposes the source `tools/audio/pixabay_music.py` through the protected loopback route `/internal/v1/media/pixabay-music`.
- The adapter preserves the source sequence and defaults: search page, bootstrap JSON, HTML MP3 fallback, 30–120 second duration filter, unfiltered fallback, first result, and MP3 download. It does not add ranking, recommendation, automatic catalog polling, or another provider.
- The Control API owns the workspace/asset transaction and stores the downloaded bytes only after actual MIME, size and SHA-256 verification. The Studio calls the route only after an explicit user query; it never auto-fills a missing BGM.
- The Runtime accepts only HTTPS `cdn.pixabay.com` audio URLs as a platform SSRF boundary. This is not a source selection change. Pixabay remains the source tool's `EXPERIMENTAL` scraper and can fail when the site structure changes.
- Actual source-path check on 2026-08-30: `ambient` returned 20 results, 4 within the default duration range, selected a 106-second track and downloaded 3,402,187 bytes; `upbeat` also completed successfully. No video Provider, TTS, Veyra or billing call was made.
- Root serial regression after restoration: `pnpm test` exited 0 with 444 passed, 18 explicit environment-gated skips and 0 failures; the chapter remains `IMPLEMENTED_PENDING_AUDIT`.

The final bullet above describing explicit-only Studio behavior is a historical snapshot. In the current minimal AUTO fallback slice, Control API may invoke this same injected Runtime route once, before a production run is created, only when the shared workspace MUSIC selector is empty. The Runtime adapter, source sequence, URL boundary, and asset verification are unchanged; Worker/Studio do not add a second Pixabay network path.

## 2026-08-30 source-first correction after deep audit (16:04)

- OpenMontage `tools/audio/piper_tts.py` exposes numeric `length_scale`; it does not define the platform's symbolic `SLOW`/`FAST` values or the former `1.15`/`0.85` mapping. The adapter now exposes only the upstream `NATURAL` default (`1.0`) and rejects unmapped symbolic pace before invoking Piper. This removes an invented parameter mapping; it does not add a new pace protocol.
- The Control API no longer reports optional `faster-whisper` or unconfigured reference vision analysis as `AVAILABLE`. `faster-whisper` is `NOT_CONFIGURED` until the controlled Media Runtime probes the same interpreter; reference vision is `AVAILABLE` only when the existing analyzer port is injected. The capability surface is now fail-closed and does not claim static dependency availability.
- Current behavior evidence after this correction: Media Runtime `94/94` (`python -m pytest -q`), OpenMontage audio adapters `7/7`, Control API `53 pass / 1 explicit service-gated skip / 0 fail`. The root `444/18/0` count remains the latest root regression before this adapter/UI capability-only correction; a fresh root run is required before treating it as post-correction evidence.
- The earlier 14:22/15:22/15:30 entries remain historical evidence. Current hard gates are unchanged: approved full-narration section windows are not consumed by Runtime, cue-only multi-cue remains blocked before Piper, complete AudioPlan semantics and transition/xfade coverage are not accepted, Studio sample/approval/TimelinePlan is incomplete, REQUIRED subtitle facts and Mandarin accent are not certified, and no automatic duration rewrite/fill is implemented.

## 2026-08-30 final deep-audit correction (17:05)

- The source mapping was rechecked against the fixed OpenMontage commit above. No new narration algorithm, timing fallback, catalog, provider protocol or UI meaning was added. Runtime request bodies now use bounded chunked reads before the existing source adapters are invoked; malformed/oversized JSON fails closed.
- The mixer graph remains source-aligned: OpenMontage `_full_mix` sidechain values are `ratio=9`, `level_sc=1`, `mix=0.9`; the platform's positive attenuation field is only converted to the source linear music-volume value and is never treated as a compressor ratio. The source `duck`/`apad`/`atrim`/`loudnorm` ordering is preserved where the platform path is enabled.
- Control API capability reporting is conservative: FFmpeg/ffprobe and Piper are `AVAILABLE` only when configured Runtime paths/model/config/launcher are visible; otherwise the capability is `NOT_CONFIGURED`. A separate Runtime probe is still required for faster-whisper/vision capabilities.
- Latest behavior evidence: Media Runtime `97 passed / 0 failed`; OpenMontage audio adapters `7/7`; Control API `53 pass / 1 explicit service-gated skip / 0 fail`; Production Worker `43/43`; root `pnpm test` `447 passed / 18 explicit environment-gated skips / 0 failed`; `pnpm typecheck`, `pnpm build`, and `git diff --check` pass (build keeps only the existing Nuxt/Nitro `DEP0155` warning).
- Earlier pace-mapping and test-count entries in this file are historical snapshots and are superseded for current verification by this section. The hard gates remain open: approved full-narration section windows are not consumed, cue-only full-narration-first and complete AudioPlan/transition semantics are not accepted, Studio sample/approval/TimelinePlan and REQUIRED subtitle fact chains are incomplete, Mandarin accent is uncertified, and overlong-narration consumer behavior remains fail-closed.

## 2026-08-30 C12.4-S01 Pixabay MIME/role boundary closeout (17:45)

- The source remains `calesthio/OpenMontage@4eab34c5cfcccaa4f1970554928feccce73ee930`, `tools/audio/pixabay_music.py`; search/bootstrap/HTML fallback, duration filter, first-match and MP3 download semantics are unchanged.
- The thin adapter now rejects a download whose declared media type is not `audio/mpeg` before writing bytes. This is the platform's existing media-input boundary; no new catalog or selection policy was introduced.
- Server-owned roles stay distinct: only imported/uploaded `AUDIO` with `metadata.audio_role=MUSIC` is eligible for AUTO music; narration samples, user source audio, and unclassified audio are excluded. A cross-role regression covers caller metadata attempting to override the role.
- Evidence: Control API `55 pass / 1 explicit service-gated skip`; adapter tests `8/8`; Runtime `97 passed / 0 failed`; root `pnpm test` `449 passed / 18 explicit environment-gated skips / 0 failed`; typecheck/build/diff check/state validator pass. No new real network/Provider/TTS/Veyra operation was performed.
- C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`; approved full-track section windows, complete AudioPlan/transition, cue-only full-narration-first, Studio approval/TimelinePlan, REQUIRED subtitle facts, Mandarin accent and overlong-consumer gates remain open.

## 2026-08-30 C12.4-S03 source-first `_full_mix` carrier closeout

- The fixed source remains `calesthio/OpenMontage@4eab34c5cfcccaa4f1970554928feccce73ee930`; `tools/audio/audio_mixer.py::_full_mix` and `_track_filters` are mirrored only in `services/media-runtime/adapters/openmontage_audio/full_mix.py`. The adapter receives bounded service-owned temporary paths and a controlled command runner; it does not expose OpenMontage project/event files, Backlot state, URLs or arbitrary commands.
- Existing ALCHMED8 is used as the private carrier. `TimelinePlan`/`ProductionCompositionInput` now allow an all-or-nothing set of explicit, non-sample formal narration asset versions per PRIMARY section. Worker reads each asset through the existing workspace/project-scoped `StoragePort`, verifies MIME/SHA/bytes/ffprobe duration and absolute window, and sends the bytes by existing track identity. No ALCHMED9, second AudioPlan, per-cue Piper call, time stretching, truncation or silence invention was added.
- Runtime maps those bytes to source `{path, role: "speech", start_seconds}` tracks, preserves source fade-before-delay, speech/music/sfx role ordering, sidechain values (`ratio=9`, `level_sc=1`, `mix=0.9`), target `apad/atrim`, and loudnorm; it remuxes the source video with the resulting full-mix track. A single legacy full narration asset remains the existing `t=0` path; cue-only multi-section and unconsumed full-track section offsets remain fail-closed.
- Behavior evidence: `python -m pytest -q tests/test_runtime.py` — `101 passed`; `python -m pytest -q adapters/openmontage_audio/test_adapters.py` — `11 passed` (including actual controlled ffmpeg/ffprobe media fixture, source precondition failures and filter graph assertions); `pnpm --filter @alchemy-video/production-worker test` — `45/45`; `pnpm --filter @alchemy-video/persistence test` — `53 pass / 10 explicit DATABASE_URL-gated skip`; contracts `36/36`; domain `43/43`; root `pnpm test` — `452 passed / 18 explicit environment-gated skips / 0 failed`; root typecheck/build pass, build retains only the existing Nuxt/Nitro `DEP0155` warning.
- C12.4-S03 passed its independent Exit Gate and is `ACCEPTED` as a slice, not as chapter acceptance. The remaining chapter hard gates are approved full-track section-level windows, complete AudioPlan semantics/identity, transition/xfade coverage, cue-only full-narration-first, Studio sample/approval/TimelinePlan, REQUIRED subtitle facts, Mandarin accent certification and overlong-narration consumer behavior. C12.4-S04 is now the only active slice and may consume only existing ALCHMED8 fields with the fixed source semantics; no real video Provider/TTS/Veyra/credit/VPS/DNS/TLS/deployment or Git operation was performed.

## 2026-08-30 C12.4-S04 AudioPlan MUSIC gain mapping

- The only S04 code correction in this checkpoint is the existing ALCHMED8 `MUSIC.gain_db` mapper in `services/media-runtime/runtime.py`: OpenMontage `_track_filters` accepts a linear `volume`, so the platform composes the authored `music_volume` with the same source-faithful `10 ** (gain_db / 20)` conversion already used for source-aligned track fields, while retaining source `start_seconds` and fades.
- Evidence: Runtime `102 passed`, including a direct full_mix input assertion, and OpenMontage adapter tests `11 passed`; state validation and diff check passed. No new public wire, magic, threshold, algorithm, fallback or external call was introduced.
- This does not close S04: transcript fields have no source `_full_mix` input and remain carried/validated for the existing downstream caption path; per-window music semantics, restart/repeat, cross-workspace and public-redaction behavior still require evidence. S04 remains `IN_PROGRESS`, and C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`.

## 2026-08-30 C12.4-S04 source-faithful boundary closeout (21:15)

- Rechecked `calesthio/OpenMontage@4eab34c5cfcccaa4f1970554928feccce73ee930`, `tools/audio/audio_mixer.py::_track_filters/_full_mix`: the source applies track-local volume/fade before absolute delay and accepts one global ducking switch; it does not consume track end times or multiple music windows.
- The platform change is limited to the existing ALCHMED8 carrier: `MUSIC.gain_db` is mapped to the source linear `volume` alongside the authored music bed level, `duck_under_narration` controls the source global ducking switch, and unsupported partial/multiple windows fail closed before calling the source mixer. No new filter graph, protocol, magic, threshold, timing rewrite or fallback was added.
- Evidence: Runtime `104 passed`, OpenMontage adapter `11 passed`, Production Worker `47/47`, Persistence `53 pass / 10 explicit DATABASE_URL-gated skip`, Contracts `36/36`, Domain `43/43`, root typecheck passed. Worker evidence covers duplicate claims, cross-workspace MUSIC rejection and safe final-review persistence. No real Provider/TTS/Veyra/network/VPS/Git operation occurred.
- Remaining S04/overall hard gates are intentionally open: transcript's actual caption/transcriber consumer (S06), full restart/repeat artifact integration, approved full-track section windows, transition/xfade coverage, cue-only full-narration-first, Studio sample/approval/TimelinePlan, REQUIRED subtitle facts, Mandarin accent and overlong consumer behavior. S04 stays `IN_PROGRESS`; C12.4/C12.5 stays `IMPLEMENTED_PENDING_AUDIT`.

## 2026-08-30 C12.4-S04 complete AudioPlan full-track handoff (22:05)

### Restart/repeat artifact evidence (22:22)

- The existing Production Worker approved-narration test now pre-seeds the deterministic `composed.mp4` object with the exact compose bytes before processing. This models a Worker restart after object storage succeeds but before `completeProductionComposition` commits.
- The existing `storeArtifact` path remains the only write path: `If-None-Match: *` is followed by MIME, byte-size and SHA-256 comparison, so an identical immutable artifact is reused; approved narration still bypasses Piper and persistence completes once. No new protocol, state, algorithm or fallback was added.
- Evidence: `pnpm --filter @alchemy-video/production-worker test` `51/51`; `pnpm --filter @alchemy-video/production-worker typecheck` PASS; combined Runtime/adapter `python -m pytest -q tests/test_runtime.py adapters/openmontage_audio/test_adapters.py` `121 passed / 0 failed`.
- Watchdog read-only review confirms this is within S04's Worker reliability gate. S04 remains `IN_PROGRESS/verifying`, and C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`; approved full-track section windows, actual transcript subtitle downstream, transition/xfade, cue-only full-narration-first, Studio approval/TimelinePlan, REQUIRED subtitles, Mandarin accent and overlong consumer gates remain open. No real Provider/TTS/Veyra/network/VPS/DNS/TLS/deployment/Git operation occurred.

- Reused the pinned OpenMontage `AudioMixer._full_mix` contract for complete ALCHMED8 full-track narration: one narration byte payload is mapped to one `speech` track covering the target, with source `start_seconds`, `volume`, fade ordering, optional `music`, and preserved source audio as `sfx`. The legacy ALCHMED1–7 path remains unchanged.
- The TypeScript encoder and Python Runtime parser now reject multiple platform narration tracks, or full-track section windows that are not one source-expressible `PRIMARY` window, before any source mixer call. This prevents silently selecting one track or inferring section offsets. No wire field, magic, filter graph, timing rewrite, or fallback was added.
- Complete ALCHMED8 full-track and independent section-track paths both invoke the same source adapter; legacy `_mix_narration_track/_mix_music_track` remains only for non-complete compatibility paths. Controlled FFmpeg/ffprobe composition produced verified MP4 with video+audio and the target duration.
- Evidence: Runtime `108 passed`, OpenMontage adapter `11 passed`, combined `119 passed`; Production Worker `51/51`; Production Worker typecheck PASS; state validation and diff check PASS. S04 remains `IN_PROGRESS`; restart/repeat artifact integration, transcript's real subtitle/transcriber downstream, approved unexpressible section offsets, transition/xfade, cue-only full-narration-first, Studio approval/TimelinePlan, REQUIRED subtitle facts, Mandarin accent and overlong consumer hard gates remain open. No real Provider/TTS/Veyra/network/VPS/Git operation occurred.
### Artifact conflict fail-closed evidence (22:49)

- The existing Worker artifact test also pre-seeds the derived `composed.mp4` key with different bytes. Existing `storeArtifact` rejects a MIME/byte-size/SHA mismatch after `If-None-Match: *`; composition persistence is not called and the lease releases with the conflict error.
- This only exercises the existing StoragePort immutable-artifact boundary. Worker `52/52` and typecheck pass; S04 remains `IN_PROGRESS/verifying`, overall C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`.

## C12.4-S04 music-only AudioPlan source handoff (2026-08-30 23:06)

- The pinned OpenMontage `AudioMixer._full_mix` accepts a music-only set of tracks and uses the same source `amix`, target-duration and normalization path when no speech track is present. The Runtime branch now sends an existing ALCHMED8 `audio_plan` with `music_bytes` through that source adapter instead of the legacy `_mix_music_track` path.
- The change is a thin branch correction only. Existing `MUSIC` ownership, absolute start, fade, dB-to-linear volume mapping, workspace/object checks and partial/multiple-window fail-closed behavior remain unchanged; no new wire, filter graph, policy, threshold or fallback was added.
- Evidence: Runtime `1/110` focused pass and `122/122` combined Runtime+adapter pass; Production Worker `53/53`; Worker typecheck, state validation and diff check pass. S04 remains `IN_PROGRESS/verifying`; source-unexpressible section offsets, transcript downstream, transition/xfade, cue-only full-narration-first, Studio/REQUIRED subtitles, Mandarin quality and overlong consumer gates remain open.

## 2026-08-31 E09 source-conformance narrow increment

- Fixed source scope is `calesthio/OpenMontage@4eab34c5cfcccaa4f1970554928feccce73ee930`: `tools/analysis/transcriber.py` (`WhisperModel`, `word_timestamps=True`, `vad_filter=True`, CPU/int8 platform subset), `tools/subtitle/subtitle_gen.py` (`_build_cues` 8-word/42-character grouping and `_hmsms` timestamp carry), and `tools/video/remotion_caption_burn.py` (`_render_ffmpeg` fallback).
- The only production correction in this increment is the source-conformance reset in `services/media-runtime/runtime.py::_subtitle_srt_from_transcript`: after a cue flush, the next cue starts from the current word, matching `SubtitleGen._build_cues`. Runtime continues to require checked word timings and does not infer timings from script text.
- The loopback caption path retains only the source FFmpeg fallback (`subtitles`, preserved audio stream, `alchemy_captions=burned_srt` marker); Remotion rendering and source VTT/JSON/correction/highlight modes remain outside the platform contract and explicitly deferred. Source transcriber model/language/GPU/diarization metadata not carried by the platform transcript contract also remains deferred.
- Focused fixture evidence (from `services/media-runtime`): `python -m pytest -q tests/test_runtime.py -k "transcriber_reports_unavailable or transcriber_capability_rejects_interpreter or transcriber_capability_probes_the_same_interpreter or transcriber_preserves_source_word_timestamp_flags_and_rounding or subtitle_srt_reuses_source_8_word_42_char_grouping_and_ms_rounding or caption_burn_uses_checked_word_timestamps_and_marks_artifact or caption_burn_requires_checked_source_timing_before_ffmpeg or caption_burn_handler_returns_marked_video_headers or caption_handler_rejects_raw_mp4_and_malformed_json or caption_handler_rejects_oversized_video_base64 or transcribe_handler_returns_source_aligned_unavailable_payload"` → `11 passed / 111 deselected / 0 failed / 0 skipped` (`2026-08-31`). Fixtures/mock only; no real model, TTS, Provider, network, Veyra, VPS or Git operation.
- This is only a source-expressed REQUIRED-caption/transcript boundary increment. It does not certify Mandarin quality or enable Remotion, VTT/JSON, GPU/diarization, script-derived timing, or a second caption protocol; C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`.

## 2026-08-31 E08 `_segmented_music` and HyperFrames timed-audio source migration (14:40)

- Source: OpenMontage `4eab34c5cfcccaa4f1970554928feccce73ee930`, `tools/audio/audio_mixer.py::AudioMixer.execute/_segmented_music`, `tools/video/hyperframes_compose.py::_resolve_audio_refs`, and `skills/core/hyperframes.md:143-162`.
- Local thin shells: `adapters/openmontage_audio/segmented_music.py::OpenMontageSegmentedMusicMixer` and `hyperframes_audio.py::OpenMontageHyperFramesAudio`, called only by internal Runtime helpers. The segmented adapter retains source parameter names, start sorting, fade expression, additive `+` overlap, `amix normalize=0`, stream mapping and AAC output. The HyperFrames adapter retains independent narration/music files, track/timing attributes, source optional/explicit-zero end fallback and volume default semantics.
- Platform-only boundary work reuses existing Runtime facts: controlled temporary paths, workspace/project scope, role, MIME, byte-size, SHA-256, ffprobe and error normalization. The existing `MediaRuntimeCompositionPlan.music_segments_ms` boundary rejects overlap after start sorting; the source adapter does not add that rule. No `_full_mix` merge, second AudioPlan, public UI/API/HTML protocol, provider path or network fetch was added.
- Verification (local fixture/mock only, no external service): adapter focused `14/14`, Runtime focused `3 passed / 122 deselected`, full Runtime `125 passed`, combined OpenMontage adapters `25/25`, and module `py_compile` pass. Controlled ffmpeg/ffprobe/WAV artifacts were actually executed. E08 source-expressed slice is `ACCEPTED` after independent acceptance; overall C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`.

## E12 real TTS and Mandarin-accent authorization boundary (2026-08-31; historical blocked snapshot, superseded)

- Source scope remains the pinned OpenMontage TTS adapter/selector files at `4eab34c5cfcccaa4f1970554928feccce73ee930`; the local Piper adapter only carries source command parameters and offline structure checks.
- At the time of this snapshot no real TTS/provider or Mandarin-accent evidence was claimed. A concrete provider/profile, authorized call count, source material, quota ceiling, credential injection plan and explicit external authorization remain required for cloud TTS/accent acceptance.
- Until those inputs exist, cloud TTS/Veyra/credit/deployment calls remain disabled and no new pace mapping, duration correction, protocol, UI or deployment path may be added. This is a historical authorization blocker, not a test pass or acceptance record; C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`.

## E12 live 30s/480p canary and local Piper verification (2026-08-31 16:05; blocked, not accepted)

- The user authorized exercising existing capabilities at 480p. The current stack used the existing Control API → Production Worker → `sub2api:grok-imagine-video-1.5` path; no Runtime adapter or source contract changed. Insurance project production `prd_01M1BD7G2CHTQPPPBA2QZSV0Q1` completed two 15-second provider segments once each and produced `848x480`, `30.084s`, H.264/AAC output. Composition QC was `PASS`; the existing workspace MUSIC selection was applied with `-14.2 LUFS`, `-1.7 dBTP`, and no unexpected silence.
- The existing Media Runtime Piper endpoint produced a measured `16250ms` mono 22050Hz WAV for the insurance script. This is one-shot local source-parameter evidence only; it does not certify Mandarin accent, full narration, or a formal TimelinePlan. Pixabay import remained an external `503 PROVIDER_UNAVAILABLE`/upstream `403`; no second scraper or fallback was introduced.
- Keep all unresolved gates fail-closed: formal narration asset/TimelinePlan and section windows, complete AudioPlan, transitions, Studio approval, REQUIRED captions, Mandarin accent, and long-narration consumer behavior. E12 remains `BLOCKED`; no code or source mapping was added by this live test.
- Root `pnpm test` (2026-08-31 20:44) completed with `470 passed / 19 explicit skips / 0 failed` across 18 workspace projects; skips remain environment-gated and no new external call was added.

## 2026-09-01 R01 voice-owner boundary (partial implementation; full gate blocked)

- Fixed source remains OpenMontage `4eab34c5cfcccaa4f1970554928feccce73ee930`. `adapters/openmontage_audio/selector.py` now mirrors the source selector's registry-first boundary: because this Runtime has no source registry, `auto`/empty selection is explicitly unavailable; only an explicitly selected `piper`/`piper_tts` uses the existing source Piper fallback. No local provider tuple, score, pace mapping or network path was added.
- `apps/production-worker/src/media-service.ts` keeps the existing QC path fail-closed when continuous narration has canonical script/cues but no approved narration bytes/tracks, so unresolved ownership cannot silently invoke Piper. Approved narration asset, Storage/SHA/byte/duration and `inspectAudio` paths remain unchanged.
- Local focused evidence: selector `2 passed / 9 deselected / 0 skipped`; Production Worker `26 passed / 0 failed / 0 skipped`; `git diff --check` no errors (only existing line-ending warnings). No Provider/TTS/network/Veyra/VPS/Git operation was performed.
- Full R01 remains blocked: the current provider profile/`VideoProviderPort` has no fixed source-backed native-audio owner, Runtime has no source registry discovery/rank positive mapping, and `packages/provider-video/src/prompt-compiler.ts:33` retains legacy PLATFORM_NARRATION dialogue-suppression semantics. Do not claim native migration, Mandarin quality or start R02 without source/contract evidence.

## 2026-09-01 E12 Doubao source-shaped mapper and authorized upstream smoke (MAPPER_ONLY; platform route blocked)

- Fixed source is OpenMontage `tools/audio/doubao_tts.py::DoubaoTTS` at commit `4eab34c5cfcccaa4f1970554928feccce73ee930`. The private `services/media-runtime/adapters/openmontage_audio/doubao.py` keeps the source submit/query URLs, `X-Api-Key`/`X-Api-Resource-Id`/`X-Api-Request-Id` headers, `req_params` audio fields, `task_id` query body, error interpretation/hints and secret redaction. It does not import an HTTP client, execute requests, register a provider, or change the Runtime contract.
- The explicit profile reads only the source environment names `DOUBAO_SPEECH_API_KEY` and `DOUBAO_SPEECH_VOICE_TYPE`; the real key remains outside the repository. The selected source resource/voice for the authorized smoke was `seed-tts-2.0` / `zh_female_vv_uranus_bigtts`. No key, token, request id or response URL is recorded here.
- Offline evidence: `python -m pytest -q adapters/openmontage_audio/test_doubao.py adapters/openmontage_audio/test_adapters.py -k "doubao or selector"` → `7 passed / 9 deselected / 0 skipped`; `py_compile` for the mapper/tests and `git diff --check` pass. These are fixture-only checks.
- User-authorized source smoke called the upstream `DoubaoTTS.execute` directly with the configured profile and produced a completed MP3 (`57372` bytes) plus source metadata (`2506` bytes). Host `ffprobe` was unavailable, so no duration claim is made. This proves only the supplied credential/resource/voice can reach the source API; it is not a platform Runtime, provider-owner, TimelinePlan or Mandarin-accent acceptance test.
- The same MP3 was later inspected offline with the repository-bundled `ffprobe-static`: codec `mp3`, `24000` Hz, mono, duration `2.856000s`. This is artifact evidence only and does not change the blocked platform status.
- Historical note: the mapper-only/route-disabled statements immediately above describe the authorization-before-route snapshot and are superseded by the explicit route evidence below. They remain retained for audit history.

## 2026-09-01 E12/R01 explicit OpenMontage Doubao Runtime route (historical VERIFYING; current BLOCKED)

- Fixed source remains OpenMontage `tools/audio/doubao_tts.py::DoubaoTTS` at commit `4eab34c5cfcccaa4f1970554928feccce73ee930`. The adapter preserves source submit→poll→download, URLs, `X-Api-*` headers, `req_params`, status/error interpretation, pre-query sleep, `(10,60)` submit/query and `(10,120)` download timeouts, source metadata write order, and voice/resource input overrides.
- The explicit thin shell is now wired only through `main.py` `preferred_provider=doubao|doubao_tts` → `runtime.py::synthesize_doubao_narration_bytes` → `DoubaoTTS.execute`. `selector.py` retains source registry-first semantics: auto/unknown is unavailable without a registry; explicit Doubao requires `DOUBAO_SPEECH_API_KEY`; explicit Piper remains the legacy offline choice. No automatic cloud selection or fallback is introduced.
- Internal loopback contract fields (`voice_id`, `resource_id`, source format/sample rate/rate/timestamp/usage/poll/timeout) and audio MIME union are private Runtime/Worker fields only. Existing workspace/object, SHA/size, ffprobe, temporary-path and secret/signed-URL redaction boundaries remain the only platform adaptation. No public DTO, AudioPlan, UI, billing or second protocol was added.
- Fresh local behavior evidence: Media Runtime + adapter tests `151 passed`, Doubao/selector adapter subset `24 passed`, Production Worker Runtime Client `23 passed`, all `0` skip/fail; `py_compile`, Worker typecheck and diff check pass. Handler tests cover explicit source fields/MIME and auto fail-closed. All fixtures use mock transports/fetchers.
- User-authorized real Runtime smoke with external key/profile `seed-tts-2.0` / `zh_female_vv_uranus_bigtts` completed source submit→poll→download and returned `audio/mpeg`, `37212` bytes, `1850ms` by bundled ffprobe. No secret, task id or URL is recorded. This is connectivity/format evidence only, not human accent, formal narration asset, TimelinePlan, native owner or chapter acceptance.
- Historical status in this section was `E12/R01=VERIFYING`; current status is `E12/R01=BLOCKED`, while `C12.4/C12.5=IMPLEMENTED_PENDING_AUDIT`. The route/smoke evidence does not constitute current acceptance or new external-call authorization. Approved section windows, complete AudioPlan, Studio sample/approval/TimelinePlan, REQUIRED captions, Mandarin listening, native owner/registry rank and overlong consumer gates remain fail-closed; do not enter R02.

## Current status recovery (2026-09-01)

- E12/R01 is currently `BLOCKED`: complete native/TTS owner, source registry/rank, formal narration asset/TimelinePlan, approved section windows and human Mandarin-accent gates remain open. The preceding route and smoke are historical evidence only.
- The Doubao adapter remains isolated and disabled by default. `apps/production-worker/src/index.ts` no longer injects `MEDIA_RUNTIME_NARRATION_PROVIDER` at worker startup; explicit controlled/test entry points remain available.
- Overall `C12.4/C12.5=IMPLEMENTED_PENDING_AUDIT`; no new provider call, protocol, algorithm or UI is authorized by this reconciliation.

## 2026-09-02 caption choice and source-aligned horizontal style

- Studio now exposes the existing `caption_policy` choice as `OFF` (default) or `REQUIRED`; the selected value is passed through the existing DeliveryPlan path. This is a UI thin shell over the current contract, not a new caption protocol. `OPTIONAL` remains contract-only because the current Worker burns captions only for `REQUIRED`.
- Runtime caption burn now uses the pinned OpenMontage horizontal fallback values from `skills/core/subtitle-sync.md`: `Arial`, font size `22`, outline `2`, shadow `1`, bottom-center `Alignment=2`, `MarginV=40`. Audio mapping and the existing `alchemy_captions=burned_srt` marker are unchanged.
- Caption text remains the checked word-timestamp transcription produced by the existing OpenMontage-aligned path. It is not silently replaced with script text; source `corrections`/manual transcript editing is not carried by the current contract and remains deferred. The Studio copy therefore asks for human text review when `REQUIRED` is selected.
- Verification (local fixtures/bundled media only): Studio project-flow `22/22`, Runtime caption focused `1/1` (`133` deselected), Studio typecheck and `git diff --check` pass; no Provider/TTS/network/Veyra/VPS/Git operation. Formal status remains unchanged (`E12/R01=BLOCKED`, overall `C12.4/C12.5=IMPLEMENTED_PENDING_AUDIT`).

## 2026-09-02 source-faithful narration text boundaries (current implementation, not acceptance)

- Fixed source remains OpenMontage `4eab34c5cfcccaa4f1970554928feccce73ee930`: `skills/meta/voice-performance-director.md` and `skills/pipelines/explainer/asset-director.md` require the provider-facing `provider_text`/delivery cues to reach the selected TTS provider; `tools/audio/piper_tts.py` and `tools/audio/doubao_tts.py` consume the authored text and provider-native pause controls. The source does not define automatic Chinese pause inference, symbolic pace-to-number mapping, or silent duration correction.
- The thin platform correction keeps authored CRLF/newline/paragraph boundaries while normalizing only horizontal formatting whitespace in `packages/domain/src/narration-quality.ts`, `packages/creative-planning/src/index.ts`, and Studio's existing quoted/labelled narration extraction. The extractor also accepts the already-supported labelled paired `”... ”` boundary used by the planner; no upload, public field, event, protocol, provider, or fallback path was added.
- Existing `provider_text` remains the sole TTS payload: Runtime's Doubao branch still receives the exact text, and the Worker still submits one canonical narration rather than one TTS job per visual cue. No pause durations are invented; explicit SSML/energy/pace requests that a selected local provider cannot honor remain fail-closed per source boundary.
- Verification (fixture/mock only): creative-planning `43/43`, domain `48/48`, Studio `40/40`, Production Worker `64/64`; Media Runtime narration focused `9 passed / 126 deselected / 0 failed / 0 skipped`; `python -m py_compile main.py runtime.py`, package typechecks, and `git diff --check` pass. No real Provider/TTS/network/Veyra/VPS/Git operation was performed in this increment.
- This closes only the text-boundary loss identified in the current chain. Human Mandarin rhythm/accent, provider-native mouth sync, formal NarrationAsset/TimelinePlan, complete AudioPlan/section windows, and restart/repeat artifact evidence remain separate hard gates; E12/R01 remains `BLOCKED` and overall C12.4/C12.5 remains `IMPLEMENTED_PENDING_AUDIT`.

## 2026-09-02 source-faithful narration prompt-boundary correction (current implementation, not acceptance)

- A second audit found the final creative-planning Prompt join still applied the legacy all-whitespace normalizer to the native spoken-script part after extraction. The thin correction preserves source line/paragraph breaks through sentence distribution and the final native Prompt join; all other prompt normalization and planning behavior remains unchanged.
- Verification: creative-planning `44/44`, domain `48/48`, Studio `40/40`, Production Worker `64/64`, Runtime narration focused `9/9`; typecheck, `py_compile`, state validation and diff check passed. Fixture/mock or bundled media only; no real Provider/TTS/network/Veyra/VPS/Git call.
- This is only a source-text boundary correction. It does not establish human Mandarin rhythm/accent, native lip sync, formal NarrationAsset/TimelinePlan, complete AudioPlan/section windows or restart/repeat artifact acceptance; `E12/R01=BLOCKED`, overall `C12.4/C12.5=IMPLEMENTED_PENDING_AUDIT` remain unchanged.

## 2026-09-02 source-faithful narration full regression audit (current implementation, not acceptance)

- Full `services/media-runtime/tests/test_runtime.py` passed `135/135`; workspace typecheck passed for all 18 participating packages/projects. Python compile, state validation and diff check also passed (the latter reports only existing LF/CRLF normalization warnings).
- The full suite confirms that preserving authored line/paragraph boundaries is compatible with the existing OpenMontage-aligned provider routes, explicit pause/SSML guards, mixer behavior and fail-closed branches. All evidence is local fixture/mock or bundled media; no real Provider/TTS/network/Veyra/VPS/Git call was made.
- This remains a narrow source-text migration audit, not acceptance of automatic Mandarin pause inference, duration correction, formal NarrationAsset/TimelinePlan, complete AudioPlan/section windows, restart/repeat artifacts or human audio quality. `E12/R01=BLOCKED` and overall `C12.4/C12.5=IMPLEMENTED_PENDING_AUDIT` remain unchanged.

## 2026-09-02 native-provider zero-word silence gate (narrow implementation)

- Fixed source remains OpenMontage `skills/pipelines/explainer/compose-director.md` at `4eab34c5cfcccaa4f1970554928feccce73ee930`: after rendering, checked transcription is mandatory; zero words means silent/missing audio and the review must stop. The platform thin shell applies this existing stop boundary during segment QC only when the private `audio_owner=NATIVE_PROVIDER` fact and persisted `delivery_cues[].provider_text` explicitly establish a speech expectation.
- `apps/production-worker/src/media-service.ts` calls the existing loopback `transcribe` operation before handoff extraction. A checked transcript with no non-empty words fails before handoff/acceptance; unavailable transcription follows the existing retryable runtime path. The exact zero-word `QC_FAILED` retains `retryable=true` so the existing segment retry command remains available, while generic `QC_FAILED` behavior is unchanged. `packages/persistence/src/production-repository.ts` keeps strict `provider_text` for this private gate and preserves the separate historical `cue.text` fallback used by composition.
- A native clip without an explicit provider-text fact (visual/BGM-only or legacy snapshot) bypasses this speech gate and stays on its existing audio-preservation path; no automatic Doubao/Piper fallback, speech scoring, `<80%` cutoff algorithm, new protocol, public field, or provider call was added.
- Verification: Production Worker full local suite `68 passed / 0 failed / 0 skipped`; Persistence native-audio suite `10 passed / 0 failed / 0 skipped`; Runtime transcriber focused suite `5 passed / 130 deselected / 0 failed / 0 skipped`; Worker typecheck and package builds pass. Fixtures/mock or bundled local media only; no real Provider/TTS/network/Veyra/VPS/Git call in this increment. This is only the source-expressed zero-word silence gate; the OpenMontage `<80%` cut-off review, full final transcript comparison, human Mandarin quality and other E12/R01 hard gates remain open. Formal state is unchanged: `E12/R01=BLOCKED`, overall `C12.4/C12.5=IMPLEMENTED_PENDING_AUDIT`.

## 2026-09-02 OpenMontage 字幕取整边界与两段成片合成回归（窄修复，不升级正式状态）

- 根因已由同一份已生成输入复现：两段视频的 `/compose` 均成功并含音轨，失败发生在 `caption_policy=REQUIRED` 的 `/captions` 阶段。OpenMontage `tools/subtitle/subtitle_gen.py::SubtitleGen._build_cues`（固定 commit `4eab34c5cfcccaa4f1970554928feccce73ee930`）直接沿用词的起止时间；Runtime 转写在三位小数取整后可能出现 `start == end`。旧边界把该来源合法取整结果误判为无效，造成 `MEDIA_RENDER_FAILED`，前端遂显示成片整理失败。
- `services/media-runtime/runtime.py::_subtitle_srt_from_transcript` 现仅拒绝倒序 `end < start`，仍拒绝非有限值、负值、越界和乱序，并继续要求 `CHECKED` 词时间戳；没有改字幕分组、时间戳生成或 REQUIRED 门禁。对应 fixture 验证了来源取整词仍按原顺序进入 SRT。
- 按源码重启本地 Runtime 后，固定项目 `prj_01M14H87JKQV2PJHRQ70JFX7FQ` 的既有两段素材执行原有 composition retry（`202`）→`SUCCEEDED`；产物 `vvr_01M1GD4R5TF04BXC9AD9NW3XHJ` 为 `30.084s`，QC=`PASS`，保留音轨和既有 BGM。未重新提交 Provider/TTS，也未新增前端协议或回退路径。
- 对该最终产物再次走现有 Runtime 转录得到 `CHECKED`、`72` 个词、`5` 个语音区间（`0.00–6.24s`、`14.79–20.03s`、`20.03–24.99s`、`25.63–29.69s`）；这证明本次成片不是无声，但也保留了 Provider 原生音频实际产生的段间空档，不能把空档自动解释为缺陷或用新算法填充。
- 验证：Runtime 字幕/烧录 focused `6 passed / 130 deselected / 0 failed / 0 skipped`，`python -m py_compile runtime.py` 通过；成功重试使用本地已存片段、MinIO 和 loopback Runtime。`E12/R01=BLOCKED`、总体 `C12.4/C12.5=IMPLEMENTED_PENDING_AUDIT` 保持不变。
- 本条只修复来源时间取整导致的成片合成 400；不等同于解决部分/过短人声、`<80%` 截断、中文听感、正式 NarrationAsset/TimelinePlan、完整 AudioPlan/section windows 或人工验收。原生 Provider 完全无词仍由既有窄门 fail-closed，部分语音质量仍需独立证据。

## 2026-09-06 heterogeneous provider media normalization (implemented; formal acceptance pending)

- Fixed source remains OpenMontage `4eab34c5cfcccaa4f1970554928feccce73ee930`: `tools/video/video_stitch.py::_probe_clip`, `_needs_normalization`, `_resolve_normalization_target`, `_normalize_clip` and `_stitch`, with the `video_compose.py::_compose` `scale + pad`, `setsar=1`, and finite silent-audio layout as the corresponding source behavior.
- Runtime composition now reads the actual video/audio compatibility facts from the existing `ffprobe` boundary, chooses the first accepted clip's actual canvas when no existing profile/target is present, and creates only request-scoped normalized files when the current Runtime output contract requires it. The normalized files use source `scale={W}:{H}:force_original_aspect_ratio=decrease,pad={W}:{H}:(ow-iw)/2:(oh-ih)/2`, preserve the existing audio ownership graph, and are never persisted over source assets.
- The generation-side path was not given a second validator or Provider mapper: the existing Worker `validateMp4Bytes`/asset fact persistence remains the source of downloaded MIME, SHA, width, height and duration. The generation-side TaskWorker→Asset behavior was not rerun in this narrow composition slice; Runtime re-probes the staged bytes as the final composition boundary.
- Local bundled-media evidence: Runtime full suite `143/143` (including heterogeneous `848x480` + `752x416` cut, mixed-audio xfade artifacts, compatibility-fact gating and temporary-file cleanup), `py_compile` pass. Existing Task Worker fixture suite also ran `38 passed / 5 skipped / 0 failed`; the five skips are its pre-existing BullMQ/Redis integration gates and are retained as skips, not converted into pass evidence. No real Provider/TTS/network/Veyra/VPS/Git call was made. This entry does not upgrade formal chapter state or claim full production E2E acceptance; cross-service generation persistence and manual real-project comparison remain separate evidence boundaries.
