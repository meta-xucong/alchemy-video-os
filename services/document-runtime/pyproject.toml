[project]
name = "alchemy-document-runtime"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn>=0.30,<1.0",
  "markitdown>=0.1.7,<0.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
