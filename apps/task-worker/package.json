{
  "name": "@alchemy-video/task-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @alchemy-video/contracts build && pnpm --filter @alchemy-video/domain build && pnpm --filter @alchemy-video/persistence build && pnpm --filter @alchemy-video/task-queue build",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm --filter @alchemy-video/contracts build && pnpm --filter @alchemy-video/domain build && pnpm --filter @alchemy-video/persistence build && pnpm --filter @alchemy-video/task-queue build",
    "test": "tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@alchemy-video/contracts": "workspace:*",
    "@alchemy-video/persistence": "workspace:*",
    "@alchemy-video/task-queue": "workspace:*",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@alchemy-video/domain": "workspace:*",
    "@types/node": "^24.0.0",
    "drizzle-orm": "^0.45.1",
    "pg": "^8.16.3",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3"
  }
}
